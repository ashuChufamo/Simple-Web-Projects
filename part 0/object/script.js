var obj = "This is a string object.",
    length = obj.length; // 24

var rep=obj.replace('object','value');

//alert(rep); // This is a string value

//alert(obj); // This is a string object 
var email = 'last@gmail.com';

var lr=obj.indexOf('is');
var rl=obj.lastIndexOf('is');
var sub= obj.substr(0,4);
window.alert(sub);

/*

var find = obj.substr();

var regexpresssion= new RegExp('string');

    
var upperCase = obj.toUpperCase(),
    lowerCase = obj.toLowerCase();


alert(upperCase);
alert(lowerCase);
alert(obj);
*/
